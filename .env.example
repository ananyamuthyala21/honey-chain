# Express server
PORT=3000
NODE_ENV=development

# Optional persistent prototype store location
HONEY_CHAIN_DATA_FILE=./data/honey-chain.json

# Frontend can use this when deployed separately; same-origin is the default
VITE_API_BASE_URL=/api
